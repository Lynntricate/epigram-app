# Epigram Web App

## Requirements
- [Docker](https://www.docker.com/get-started) installed and running

## Starting the Application
Frontend, backend and database are containerized with Docker. To start the app in one command, run:

```bash
docker compose up --build 



